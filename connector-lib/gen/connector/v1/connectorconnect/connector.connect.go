// proto/connector/connector.proto

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: connector/v1/connector.proto

package connectorconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/InteractionLabs/traversal-connector/connector-lib/gen/connector/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ConnectorServiceName is the fully-qualified name of the ConnectorService service.
	ConnectorServiceName = "connector.v1.ConnectorService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ConnectorServiceTunnelProcedure is the fully-qualified name of the ConnectorService's Tunnel RPC.
	ConnectorServiceTunnelProcedure = "/connector.v1.ConnectorService/Tunnel"
)

// ConnectorServiceClient is a client for the connector.v1.ConnectorService service.
type ConnectorServiceClient interface {
	// Bidirectional streaming RPC for tunnel communication between connector and controller
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	Tunnel(context.Context) *connect.BidiStreamForClient[v1.ConnectorMessage, v1.ControllerMessage]
}

// NewConnectorServiceClient constructs a client for the connector.v1.ConnectorService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewConnectorServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ConnectorServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	connectorServiceMethods := v1.File_connector_v1_connector_proto.Services().ByName("ConnectorService").Methods()
	return &connectorServiceClient{
		tunnel: connect.NewClient[v1.ConnectorMessage, v1.ControllerMessage](
			httpClient,
			baseURL+ConnectorServiceTunnelProcedure,
			connect.WithSchema(connectorServiceMethods.ByName("Tunnel")),
			connect.WithClientOptions(opts...),
		),
	}
}

// connectorServiceClient implements ConnectorServiceClient.
type connectorServiceClient struct {
	tunnel *connect.Client[v1.ConnectorMessage, v1.ControllerMessage]
}

// Tunnel calls connector.v1.ConnectorService.Tunnel.
func (c *connectorServiceClient) Tunnel(ctx context.Context) *connect.BidiStreamForClient[v1.ConnectorMessage, v1.ControllerMessage] {
	return c.tunnel.CallBidiStream(ctx)
}

// ConnectorServiceHandler is an implementation of the connector.v1.ConnectorService service.
type ConnectorServiceHandler interface {
	// Bidirectional streaming RPC for tunnel communication between connector and controller
	// buf:lint:ignore RPC_REQUEST_STANDARD_NAME
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	Tunnel(context.Context, *connect.BidiStream[v1.ConnectorMessage, v1.ControllerMessage]) error
}

// NewConnectorServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewConnectorServiceHandler(svc ConnectorServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	connectorServiceMethods := v1.File_connector_v1_connector_proto.Services().ByName("ConnectorService").Methods()
	connectorServiceTunnelHandler := connect.NewBidiStreamHandler(
		ConnectorServiceTunnelProcedure,
		svc.Tunnel,
		connect.WithSchema(connectorServiceMethods.ByName("Tunnel")),
		connect.WithHandlerOptions(opts...),
	)
	return "/connector.v1.ConnectorService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ConnectorServiceTunnelProcedure:
			connectorServiceTunnelHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedConnectorServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedConnectorServiceHandler struct{}

func (UnimplementedConnectorServiceHandler) Tunnel(context.Context, *connect.BidiStream[v1.ConnectorMessage, v1.ControllerMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("connector.v1.ConnectorService.Tunnel is not implemented"))
}
