name: dependency-policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  dependency-policy:
    permissions:
      contents: read
      id-token: write
    uses: InteractionLabs/traversal-connector/.github/workflows/reusable-dependency-policy.yml@3303ca6374a6b8f3dfe2b2ed1f84b8acf01f8dc1 # policy-v1
