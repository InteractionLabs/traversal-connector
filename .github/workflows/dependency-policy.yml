name: dependency-policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  dependency-policy:
    uses: InteractionLabs/traversal-connector/.github/workflows/reusable-dependency-policy.yml@09d573adb0fa9cbd7ea272fd91bb8df7ccde6bfe # policy-v1
