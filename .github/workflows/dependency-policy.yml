name: dependency-policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  dependency-cooldown:
    permissions:
      contents: read
      id-token: write
    uses: InteractionLabs/traversal-connector/.github/workflows/reusable-dependency-policy.yml@eda5f7fd4bdd1b930d7664d3dadc89e6990d730c # policy-v1
