name: dependency-cooldown

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  dependency-cooldown:
    permissions:
      contents: read
      id-token: write
    uses: InteractionLabs/infrastructure/.github/workflows/dependency-cooldown.yaml@f28dca6cfb0f6c4bc5fce4991c4bb25b8cb93809 # policy-v1
